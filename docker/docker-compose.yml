services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file: ../.env
    volumes:
      - ../data:/app/data
      - ../config.yaml:/app/config.yaml:ro
    restart: unless-stopped
