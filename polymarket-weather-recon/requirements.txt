# Phase 0 needs only the standard library + these two (both already present in
# the sandbox). Heavier analysis deps are listed but commented until the phase
# that needs them, to keep the Phase-0 footprint minimal and installs fast.
requests>=2.31
PyYAML>=6.0

# --- enable per phase ---
# pandas>=2.0          # Phase 3 feature engineering
# pyarrow>=14          # parquet exports
# duckdb>=0.10         # optional analytics layer over sqlite/parquet
# scikit-learn>=1.3    # Phase 4 clustering
# hdbscan>=0.8         # Phase 4 density clustering
# matplotlib>=3.7      # Phase 5 dossier plots
# web3>=6.0            # Phase 2 on-chain decode (ONLY if a Polygon RPC is allowlisted)
# httpx>=0.27          # optional async client for live capture (Phase 8)
