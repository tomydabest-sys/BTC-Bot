"""Phase 7 — BTC-Bot recommendations (PROPOSALS ONLY) (STUB).

Write reports/btc_bot_recommendations.md mapping findings to prioritised
proposals (maker-quoting params, fee/rebate-aware placement, latency targets,
sizing adjustments, market-selection filters), each with evidence, expected
effect, implementation sketch and risk/assumptions. Also emit machine-readable
insight artifacts (JSON/SQLite) BTC-Bot could later consume.

HARD CONSTRAINT: read-only w.r.t. BTC-Bot. Do NOT import from, write to, or
call BTC-Bot's runtime. End by asking the operator which proposals to take
forward.
"""
from __future__ import annotations


def build_recommendations(*args, **kwargs):
    raise NotImplementedError("Phase 7 — pending Phases 5/6")
