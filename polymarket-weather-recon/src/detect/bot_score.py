"""Phase 4 — heuristic bot score (STUB).

Transparent weighted combination of automation features (timing regularity,
24/7 activity, low size entropy, breadth, fixed-offset pricing, fast reaction
latency). Output a 0-1 score plus per-component contributions so it is
auditable. Thresholds live in config.yaml -> thresholds.bot_score.
"""
from __future__ import annotations


def score_wallets(*args, **kwargs):
    raise NotImplementedError("Phase 4 — pending Phase 2 checkpoint")
