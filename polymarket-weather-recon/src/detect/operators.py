"""Phase 4 — operator grouping (STUB).

Link multiple wallets to one operator via near-identical sizing/timing
fingerprints and co-movement (entering the same markets within tight windows).

Caveat from Phase 0: shared-funding-source linkage needs on-chain data, which
is currently BLOCKED. Operator grouping is therefore limited to behavioural
fingerprints + co-timing until a Polygon RPC is allowlisted.
"""
from __future__ import annotations


def group_operators(*args, **kwargs):
    raise NotImplementedError("Phase 4 — pending Phase 2 checkpoint")
