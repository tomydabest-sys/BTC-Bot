"""Phase 4 — unsupervised wallet clustering (STUB).

Standardise features, cluster (HDBSCAN / k-means + silhouette), characterise
clusters, and cross-check against the heuristic bot score (agreement raises
confidence; disagreement gets flagged). Config: thresholds.clustering.
"""
from __future__ import annotations


def cluster_wallets(*args, **kwargs):
    raise NotImplementedError("Phase 4 — pending Phase 2 checkpoint")
