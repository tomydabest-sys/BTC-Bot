"""Phase 3.5 — reaction-latency features (STUB).

For each meaningful move in the reference weather signal (forecast revision /
observation crossing a market-relevant threshold), measure how fast and how
directionally-accurately each wallet trades: median/percentile latency, hit
rate, directional accuracy. Fast + consistent + directionally-correct =>
feed-driven bot, and this measures its latency budget.
"""
from __future__ import annotations


def reaction_features(*args, **kwargs):
    raise NotImplementedError("Phase 3.5 — pending checkpoints")
