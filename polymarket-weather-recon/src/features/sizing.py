"""Phase 3 — sizing features (STUB).

Size discreteness (distinct sizes, fraction round/repeating fixed notional);
size<->edge correlation (does size scale with distance-from-fair => model-driven
sizing such as Kelly/vol-scaled).
"""
from __future__ import annotations


def sizing_features(*args, **kwargs):
    raise NotImplementedError("Phase 3 — pending checkpoints")
