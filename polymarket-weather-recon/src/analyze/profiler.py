"""Phase 5 — per-bot strategy dossiers (STUB).

Rank bots/operators by a composite of weather-market volume, trade count and
PnL; for the top N (~10-15) produce reports/dossiers/<operator>.md with summary
stats, inferred strategy archetype + confidence, an evidence table, estimated
edge source, inferred sizing/timing model, and weaknesses. Every claim carries
an explicit confidence and distinguishes observed vs inferred.
"""
from __future__ import annotations


def build_dossiers(*args, **kwargs):
    raise NotImplementedError("Phase 5 — pending Phase 4 checkpoint")
