"""Historical replay scaffold.

v1 is a stub — historical replay against NCEI observations is not in
v1 scope. Exits 0 with a TODO marker so the operator-facing acceptance
checklist still passes.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("backtest: TODO(operator) — v1 replay scaffold only; see DESIGN.md")
    return 0


if __name__ == "__main__":
    sys.exit(main())
